# Qué NO entra al contexto de build de Docker. Sin este archivo, COPY . .
# arrastra todo: node_modules (se instalan adentro), builds viejos y — lo
# grave — el .env con secretos, que quedaría en una capa de la imagen.
node_modules
dist
coverage
uploads
test
.env
.env.*
# El historial completo del repo no pinta nada en la imagen — y la etapa
# "build" NO es descartable: la usa el servicio migrate del compose.
.git
*.md
.playwright-mcp
Dockerfile
.dockerignore
